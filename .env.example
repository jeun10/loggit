# PostgreSQL (docker-compose, backend local profile)
DB_NAME=loggit
DB_USERNAME=loggit
DB_PASSWORD=

# GitHub OAuth (미구현, 추후 사용)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# 저장된 GitHub 토큰 암호화 키 (미구현, 추후 사용)
TOKEN_ENCRYPTION_KEY=

# Gemini 요약 (미구현, 추후 사용)
GEMINI_API_KEY=
